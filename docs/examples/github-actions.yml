name: DevDoctor

on:
  push:
  pull_request:

jobs:
  devdoctor:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: rtcoder/devdoctor@v1.0.0
        with:
          version: v1.0.0
          args: ci --format=sarif
